class Position:
    def __init__(self, row, column):
        """
        Khởi tạo đối tượng Position với hàng và cột xác định.

        Args:
            row (int): Chỉ số hàng.
            column (int): Chỉ số cột.
        """
        self.row = row
        self.column = column


help(Position)